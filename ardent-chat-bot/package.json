{
  "name": "blog-post-bot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.0.1"
  },
  "engines": {
    "node": "12"
  }
}
